# MSTV2 - Платформа для записи на услуги

MSTV2 — это P2P платформа, которая позволяет мастерам создавать свои услуги, управлять расписанием и принимать записи от клиентов. Клиенты могут просматривать доступные услуги, выбирать удобное время, записываться на услуги и оплачивать их через ЮKassa с системой удержания средств до подтверждения выполнения работы.

## 🚀 Основные возможности

### Для мастеров:

- Создание и управление услугами
- Управление расписанием (шаблоны и конкретные даты)
- Принятие/отклонение записей от клиентов
- Подтверждение готовности выполненной работы
- Настройка счетов для получения выплат
- Чат с клиентами по услугам
- Получение уведомлений в реальном времени

### Для клиентов:

- Просмотр каталога услуг
- Запись на услуги с выбором времени
- Оплата услуг через ЮKassa
- Подтверждение выполненной работы
- Чат с мастерами
- Просмотр истории записей

### Платежная система:

- P2P модель с удержанием средств
- Интеграция с ЮKassa
- Автоматическое распределение средств (90% мастеру, 10% платформе)
- Поддержка различных способов выплат (карта, банк, ЮMoney, телефон)

## 🛠 Технологии

### Backend:

- **FastAPI** — веб-фреймворк
- **SQLAlchemy** — ORM для работы с БД
- **SQLite** — база данных (можно заменить на PostgreSQL)
- **Alembic** — миграции БД
- **Pydantic** — валидация данных
- **JWT** — аутентификация через httpOnly cookies
- **WebSockets** — реальное время для чатов и уведомлений
- **Celery + Redis** — фоновые задачи
- **YooKassa API** — платежный шлюз
- **Aiogram** — Telegram бот

### Frontend:

- **React 19** — UI библиотека
- **TypeScript** — типизация
- **Vite** — сборщик
- **React Router** — маршрутизация
- **Zustand** — управление состоянием
- **Axios** — HTTP клиент
- **TanStack Query** — кэширование и синхронизация данных
- **Tailwind CSS** — стилизация
- **Lucide React** — иконки

## 📁 Структура проекта

```
MSTV2-1/
├── server/                 # Backend (FastAPI)
│   ├── accounts/           # Управление счетами для выплат
│   ├── chats/              # Чат с клиентами и поддержкой
│   ├── common/             # Общие утилиты и конфигурация
│   │   ├── db/            # Модели БД и конфигурация
│   │   └── utils/         # JWT, логирование, ЮKassa и др.
│   ├── dates/              # Управление датами услуг
│   ├── enrolls/            # Записи на услуги
│   ├── payments/           # Платежи и выплаты
│   ├── scheduletemplates/  # Шаблоны расписания
│   ├── services/           # Услуги мастеров
│   ├── tags/               # Теги/категории услуг
│   ├── users/              # Пользователи и аутентификация
│   └── websockets/         # WebSocket endpoints
├── client/                 # Frontend (React)
│   └── src/
│       ├── api/            # API клиенты
│       ├── components/     # UI компоненты
│       ├── features/       # Бизнес-логика
│       ├── pages/          # Страницы
│       ├── stores/         # Zustand stores
│       └── types/          # TypeScript типы
├── bot/                    # Telegram бот (Aiogram)
├── migrations/             # Alembic миграции
├── run_server.py           # Точка входа для сервера
├── run_bot.py              # Точка входа для бота
└── pyproject.toml          # Python зависимости
```

## 🔧 Установка и запуск

### Требования

- Python 3.13+
- Node.js 18+
- Redis (для rate limiting и Celery)
- Poetry (для управления Python зависимостями)

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd MSTV2-1
```

### 2. Настройка Backend

#### Установка зависимостей

```bash
# Установка Poetry (если не установлен)
pip install poetry

# Установка зависимостей
poetry install

# Активация виртуального окружения
poetry shell
```

#### Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
# База данных
DB_URL=sqlite+aiosqlite:///./mstv2.db

# JWT
JWT_SECRET=your-secret-key-here
ALGORITHM=HS256
ACCESS_EXPIRE=7
REFRESH_EXPIRE=30

# ЮKassa
YUKASSA_SHOP_ID=your-shop-id
YUKASSA_SECRET_KEY=your-secret-key
DEVELOPER_CARD=your-card-number  # Карта для получения комиссии платформы

# Redis
REDIS_BACKEND=redis://localhost:6379/0

# Rate Limiting
DEFAULT_RATE_LIMITER=100/hour
AUTH_RATE_LIMITER=5/minute
ENROLL_RATE_LIMITER=10/minute
SERVICE_RATE_LIMITER=20/minute

# CORS
ALLOWED_ORIGINS=http://localhost:5173
ENVIRONMENT=development

# Cloudflare Turnstile (опционально)
TURNSTILE_SECRET_KEY=your-turnstile-secret

# Telegram Bot (опционально)
TELEGRAM_BOT_TOKEN=your-bot-token
```

#### Применение миграций

```bash
alembic upgrade head
```

#### Запуск сервера

```bash
python run_server.py
```

Сервер запустится на `http://localhost:8000`

API документация доступна по адресу: `http://localhost:8000/docs`

### 3. Настройка Frontend

```bash
cd client

# Установка зависимостей
npm install

# Запуск dev сервера
npm run dev
```

Frontend запустится на `http://localhost:5173`

### 4. Запуск Telegram бота (опционально)

```bash
python run_bot.py
```

### 5. Запуск Redis (для rate limiting)

```bash
# Windows (с Chocolatey)
choco install redis-64

# Linux/Mac
redis-server

# Или через Docker
docker run -d -p 6379:6379 redis
```

## 📚 API Endpoints

### Аутентификация

- `POST /api/v1/auth/register` — регистрация
- `POST /api/v1/auth/token` — вход
- `POST /api/v1/auth/refresh` — обновление токена
- `GET /api/v1/auth/check` — проверка авторизации
- `DELETE /api/v1/auth/logout` — выход

### Услуги

- `GET /api/v1/services/` — список услуг
- `POST /api/v1/services/` — создание услуги
- `GET /api/v1/services/{id}` — детали услуги
- `PATCH /api/v1/services/{id}` — обновление услуги

### Записи

- `POST /api/v1/enrolls/` — записаться на услугу
- `POST /api/v1/enrolls/{id}/accept` — принять запись (мастер)
- `POST /api/v1/enrolls/{id}/complete` — подтвердить готовность (мастер)
- `POST /api/v1/enrolls/{id}/confirm` — подтвердить заказ (клиент)
- `POST /api/v1/enrolls/{id}/cancel` — отменить запись

### Платежи

- `POST /api/v1/payments/` — создать платеж
- `GET /api/v1/payments/` — список платежей
- `GET /api/v1/payments/{id}/status` — статус платежа
- `POST /api/v1/payments/webhook` — webhook от ЮKassa

### Счета

- `GET /api/v1/accounts/` — получить счет пользователя
- `POST /api/v1/accounts/` — создать счет
- `PATCH /api/v1/accounts/` — обновить счет

### WebSocket

- `ws://localhost:8000/ws/service-chats/{chat_id}` — чат по услуге
- `ws://localhost:8000/ws/support-chats/{chat_id}` — чат с поддержкой
- `ws://localhost:8000/ws/notifications` — уведомления

Полная документация API доступна по адресу `/docs` после запуска сервера.

## 💳 Платежная система

Система реализует P2P модель с удержанием средств:

1. **Создание платежа** — клиент создает платеж, деньги списываются, но остаются на удержании
2. **Принятие мастером** — мастер принимает запись
3. **Подтверждение готовности** — мастер подтверждает готовность работы
4. **Подтверждение клиентом** — клиент подтверждает выполнение, средства распределяются:
   - 90% мастеру (минус 6% налог = 84.6% чистыми)
   - 10% платформе

Подробная документация: [server/payments/PAYMENT_P2P_DOCUMENTATION.md](server/payments/PAYMENT_P2P_DOCUMENTATION.md)

## 🔐 Безопасность

- JWT токены в httpOnly cookies
- Rate limiting на всех endpoints
- Валидация данных через Pydantic
- Проверка подписи webhook от ЮKassa
- CORS настройки
- Защита от SQL инъекций через SQLAlchemy

## 📝 Статусы записей

- `pending` — ожидает подтверждения мастера
- `confirmed` — принято мастером
- `ready` — готово к подтверждению клиентом (мастер подтвердил готовность)
- `completed` — завершено (клиент подтвердил, средства распределены)
- `cancelled` — отменено
- `expired` — истекло

## 🧪 Разработка

### Запуск в режиме разработки

Backend автоматически перезагружается при изменениях (reload=True).

Frontend использует Vite HMR для горячей перезагрузки.

### Миграции БД

```bash
# Создание новой миграции
alembic revision --autogenerate -m "описание изменений"

# Применение миграций
alembic upgrade head

# Откат миграции
alembic downgrade -1
```

### Логирование

Логи сохраняются в `logs/app.log`

## 📄 Лицензия

Проект разработан для внутреннего использования.

## 👤 Автор

AlexgDevs (asmetanin014@gmail.com)

---

**Версия:** 2.0.0
